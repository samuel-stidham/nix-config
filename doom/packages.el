;; -*- no-byte-compile: t; -*-
;;; packages.el

;; Extra Emacs packages beyond what the enabled Doom modules pull in. Add lines
;; like the example below, then run `doom sync`. Left empty for now, since the
;; modules in init.el already cover a first Doom setup.

;; Catppuccin Frappe theme, selected in config.el.
(package! catppuccin-theme)

;; (package! some-package)
